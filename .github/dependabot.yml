version: 2
open-pull-requests-limit: 10

updates:
  # npm dependencies (package.json / bun.lock)
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      timezone: "Europe/Oslo"
    reviewers:
      - "frodeste"
    labels:
      - "dependencies"
    commit-message:
      prefix: "chore"
      include: "scope"
    groups:
      react:
        patterns:
          - "react"
          - "react-dom"
          - "@types/react"
          - "@types/react-dom"
      next:
        patterns:
          - "next"
          - "@sentry/nextjs"
      drizzle:
        patterns:
          - "drizzle-orm"
          - "drizzle-kit"
      dev-tools:
        patterns:
          - "@biomejs/*"
          - "typescript"
          - "@types/node"
          - "semantic-release"
          - "@semantic-release/*"

  # GitHub Actions
  - package-ecosystem: "github-actions"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      timezone: "Europe/Oslo"
    reviewers:
      - "frodeste"
    labels:
      - "dependencies"
      - "ci"
    commit-message:
      prefix: "ci"
      include: "scope"
    groups:
      github-actions:
        patterns:
          - "*"

  # Dev Container Features (base image + all tool features)
  - package-ecosystem: "devcontainers"
    directory: "/"
    schedule:
      interval: "weekly"
      day: "monday"
      timezone: "Europe/Oslo"
    reviewers:
      - "frodeste"
    labels:
      - "dependencies"
      - "devcontainer"
    commit-message:
      prefix: "chore"
      include: "scope"
